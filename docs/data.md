# Data

This repository does not ship datasets. Provide your own tokenized text and optional observation vectors.

## Toy data
Use `scripts.make_dummy_data` to generate a small tensor dataset:

```bash
python -m scripts.make_dummy_data --output data.pt
```
